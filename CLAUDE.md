# (주)우주특수산업 — worker-counter

현장 작업량 데이터를 모아 ERP 전표로 올리고 현황판으로 보는 시스템.
여기에 3D 프린터 출력물 생성기가 붙어 있다.

## 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 작업자 앱 — 작업량 입력 / 휴가 / 대기 / 전송 |
| `admin.html` | 관리자 — 대시보드 · 제출 · 근로자 · 품번 마스터 · 휴가 · 공지 |
| `dashboard.html` | 생산현황 현황판 (차종 × 공정 매트릭스) |
| `worker_counter.html` | 작업자 앱 구버전 |
| `print3d.html` | 품번 라벨판 STL 생성 (브라우저) |
| `cardcase.html` | 명함 케이스 (브라우저, 개발 중단) |
| `tools/` | **3D 출력물 생성기 — `tools/README.md` 먼저 읽을 것** |
| `assets/`, `woojoo-logo.svg` | AIRBAG 라벨 4색 래스터, 회사 로고 |

데이터는 Supabase `submissions` 테이블. 한 번 전송 = 같은 `batch_id`.

## 3D 출력물

설치·실행·인자·현재 설계값은 **`tools/README.md`** 에 있다. 요약:

- `tools/lib/solid.mjs` — 다중구간 하이트맵 엔진 (벽 관통 구멍·언더컷 가능)
- `tools/lib/threemf.mjs` — 파트별 필라멘트 지정이 되는 3MF 쓰기
- `tools/build-qicase.mjs` — 무선충전 보조배터리 케이스 (현행 v2)
- `tools/build-cardcase.mjs` — 명함 케이스
- `tools/stl-preview.mjs` — Z버퍼 음영 렌더

### 이 폴더의 원칙

**치수를 눈으로 맞추지 말고 숫자로 검증한다.**
부피(발산정리) · 법선합 · 단면 탐침 · 베드 접촉 면적 · 획 두께(오프닝 테스트).
빌드할 때마다 찍히는 `베드 접촉 / 실걸림 / ✅ 닫힌 메시` 세 줄을 반드시 확인.

### 출력물 만들 때 지킬 것 (전부 실패해서 얻은 것들)

- **몸체 STL 만 슬라이싱하지 말 것.** 색 파트가 빠지면 베드 접촉이 5% 로 떨어져
  첫 층이 허공을 가로지르며 뭉개진다. 반드시 3MF (전 파트)
- **라벨·로고는 "색이 들어가는 자리만" 파낸다.** 실루엣 전체를 파면
  흰 바탕이 빈 홈이 된다
- **베드에 닿는 면의 그림은 X 로 뒤집는다.** 안 그러면 완성품에서 거울로 보인다
- **USB-C 개구는 가로 8.9mm 아래로 못 내린다** (플러그 쉘 규격 8.34mm)
- **0.4mm 노즐은 획 0.4mm 미만을 못 깐다.** 작은 글자는 오프닝 테스트로 먼저 확인
- **H2D 는 좌우 노즐 직경이 다르면 단일 헤드 출력만 된다** (슬라이서가 막는다)
- Float32Array 와 `===` 로 비교하는 상수는 `Math.fround()` 를 거칠 것

### 무선충전 케이스 v2 확정값

외형 65.9 × 98.9 × 14.3mm · 모듈 자리 60.7 × 93.7 × 12.3 (모듈 실측 93mm)
체결은 커버 안쪽 립의 비드 ↔ 셸 안쪽벽 홈. **실걸림 0.43~0.45mm, 걸림면 수평 0도**
→ 닫히지만 공구 없이는 안 열린다. **모듈 먼저 넣고 닫을 것.**
셸 바닥 = AIRBAG 라벨 4색, 커버 윗면 = 우주 로고 2색. 둘 다 베드 접촉면 → 서포트 불필요.
**본 출력 전 `qi_porttest.stl` (5분) 로 포트 위치를 반드시 확인.**

## 현재 상태 (2026-09-20)

설계 완료 · 출력 대기: 명판(이해동·김용현) / 보조배터리 케이스 추가분 /
벽면 진열장 20개 / 양면테이프 지그(이형지 분리 탈착기형).

### 미해결 — 특이사항 별도 보고

작업자가 설비·자재·품질·안전 이상을 올릴 창구가 **아직 없다.**
`메모`·`비고` 는 휴가 결재에만 있고, 작업량 제출(`submissions`)에는
자유 기재 칸이 없다. 불량코드는 숫자로 떨어지는 불량일 뿐이다.

제안된 방향: 별도 `issues` 테이블 + 작업자 앱 `특이사항` 탭(구분·내용·사진)
+ 관리자 미확인 배지 + 대시보드 상단 띠. 집계가 더러워지지 않도록 작업량과 분리.
Supabase 테이블 생성 SQL 은 사용자가 직접 실행해야 한다.
